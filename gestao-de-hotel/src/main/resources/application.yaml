spring:
  application:
    name: gestao-de-hotel

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    open-in-view: false

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml